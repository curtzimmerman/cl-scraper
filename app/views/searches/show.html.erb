<% provide(:title, @search.title.titleize) %>

<div class="content">
	<h1 class="heading center"> <%= @search.title.titleize %> </h1>
	<%= link_to "Back to Searches...", user_searches_path, class: "subheading" %>

	<table>
		<th>Title</th>
		<th>Price</th>
		<th>Location</th>
		<th>Neighborhood</th>
		<th>URL</th>
		<%= render partial: 'hits/hit', collection: @search.hits %>
	</table>
</div>